const helpText = `
  --help - Help
  -t - The site URL for collecting links
  -o <SAVE_PATH> - Save the collected links to a file (default file path - ./site-link-list) 
  -l - Enable loging collections in console
`;

export default helpText;
